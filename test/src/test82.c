/* A very simple function to test memory stores. */

int test_buffer(const char* buffer) {
    return 0;
}

int main() {
    //char str[] = "Hello, World - declared\n";

    //strlen(str);

    test_buffer("Hello, World!\n");
    return 0;
}
